/**
 * /refer-out/in — Referral detail page (refer-out view)
 *
 * This route uses the exact same component as /request-refer-out/in.
 * Both Nuxt routes (/refer-out/in and /request-refer-out/in) share the same
 * API endpoint (findOne) and identical UI layout.
 */
export { default } from "@/app/request-refer-out/in/page";
