/**
 * /refer-back/in — Referral detail page (refer-back view)
 *
 * This route uses the exact same component as /request-refer-out/in.
 * Both Nuxt routes share the same API endpoint (findOne) and identical UI layout.
 */
export { default } from "@/app/request-refer-out/in/page";
